

static func test_1() -> void:
	var board_state: = BoardState.new()
	board_state.tiles[Vector2i(0, 0)] = Tile.new()

static func add_tile_to_board_state(pos: Vector2i, state: BoardState) -> void:
	var tile: = Tile.new()
	tile.
